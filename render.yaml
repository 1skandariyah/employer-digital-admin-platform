services:
  - type: web
    name: employer-digital-admin-platform
    runtime: python
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: HOST
        value: 0.0.0.0
